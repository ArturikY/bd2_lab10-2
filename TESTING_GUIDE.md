# Руководство по проверке каждого задания

## ⚠️ ВАЖНО: Перед проверкой

1. **Обновите базу данных:**
   ```bash
   mysql -u root -p bd_lab10-2 < database_schema_update.sql
   ```

2. **Запустите PHP сервер:**
   ```bash
   php -S localhost:8000
   ```

3. **Убедитесь, что в базе есть данные:**
   - Устройство с ID=1 (MyDevice)
   - Можно добавить температуру через: `http://localhost:8000/device_status.php?ID=1&Term=25&Rele=1`

---

## 📋 Задание 1: Базовое веб-приложение

**URL:** `http://localhost:8000/index.php`

### Что вы должны увидеть:
1. ✅ **Заголовок страницы:** "MyApp"
2. ✅ **Таблица с устройством:**
   - Строка: "Устройство:" и имя устройства (например, "MyDevice" или "?")
3. ✅ **Таблица с данными:**
   - Строка 1: "Температура" | значение | дата/время
   - Строка 2: "Реле" | значение | дата/время
4. ✅ **Две кнопки управления:**
   - "Включить реле"
   - "Выключить реле"

### Как проверить:
- **Нажмите "Включить реле"** → должна обновиться страница
- **Нажмите "Выключить реле"** → должна обновиться страница
- **Проверьте команду устройству:** откройте `device_status.php?ID=1` → должно быть `COMMAND 1 EOC` или `COMMAND 0 EOC`

### Что проверить в БД:
```sql
SELECT * FROM command_table WHERE DEVICE_ID = 1;
```
Должна быть запись с последней командой.

---

## 📋 Задание 2: Разделение на модули

**URL:** `http://localhost:8000/task2_index.php`

### Что вы должны увидеть:
1. ✅ **То же самое, что в задании 1**, но:
   - **Более аккуратный интерфейс** (стили CSS)
   - **Заголовок:** "Устройство: [имя] (ID: 1)" в рамке

### Как проверить:
- Проверьте, что функционал **идентичен заданию 1**
- **Убедитесь, что подключение к БД и шаблон в отдельных файлах:**
  - Откройте `config.php` → должен быть код подключения к БД
  - Откройте `template.php` → должна быть функция `renderDeviceTemplate()` и `renderFullPage()`

### Отличие от задания 1:
- Код разделен на модули (config.php, template.php)
- Более стилизованный интерфейс

---

## 📋 Задание 3: Несколько устройств

**URL:** `http://localhost:8000/task3_index.php`

### Что вы должны увидеть:
1. ✅ **Заголовок:** "Управление устройствами"
2. ✅ **Несколько блоков устройств** (если в БД есть несколько устройств):
   - Блок 1: Устройство 1 с данными и кнопками
   - Блок 2: Устройство 2 с данными и кнопками
   - Блок 3: Устройство 3 с данными и кнопками
3. ✅ **Каждый блок содержит:**
   - Название устройства с ID
   - Таблицу с температурой и реле
   - Кнопки "Включить реле" и "Выключить реле" **для каждого устройства отдельно**

### Как проверить:
- **Добавьте данные для устройств:**
  ```
  device_status.php?ID=1&Term=25&Rele=1
  device_status.php?ID=2&Term=30&Rele=0
  device_status.php?ID=3&Term=20&Rele=1
  ```
- **Обновите страницу** → должны увидеть все 3 устройства
- **Нажмите кнопки для каждого устройства** → команды должны отправляться только для выбранного устройства

### Что проверить в БД:
```sql
SELECT DEVICE_ID FROM device_table;
```
Должны быть устройства с ID 1, 2, 3.

---

## 📋 Задание 4: Идентификация по токену

**URL:** `http://localhost:8000/task4_index.php?device_token=token_device_1`

### Что вы должны увидеть:

**Вариант 1 (с токеном):**
- ✅ **Только одно устройство** (соответствующее токену)
- ✅ Устройство отображается с данными и кнопками

**Вариант 2 (без токена):**
- ✅ **Все устройства** (как в задании 3)
- ✅ Или сообщение "Устройство не найдено. Используйте параметр device_token в URL."

### Как проверить:
- **С токеном устройства 1:** `task4_index.php?device_token=token_device_1` → должно показать только устройство 1
- **С токеном устройства 2:** `task4_index.php?device_token=token_device_2` → должно показать только устройство 2
- **Без токена:** `task4_index.php` → должны увидеть все устройства или сообщение
- **Проверьте сессию:** откройте с токеном, затем откройте без параметра → должно сохранить выбранное устройство

### Что проверить в БД:
```sql
SELECT DEVICE_ID, DEVICE_TOKEN FROM device_table;
```
Должны быть токены для каждого устройства.

---

## 📋 Задание 5: История действий

**URL:** `http://localhost:8000/task5_index.php`

### Что вы должны увидеть:
1. ✅ **То же, что в задании 3** (несколько устройств)
2. ✅ **НО! Под каждым устройством есть ссылка:**
   - "История управления устройством"

### Как проверить:

**Шаг 1: Выполните действия**
- Откройте `task5_index.php`
- Нажмите несколько раз "Включить реле" и "Выключить реле" для разных устройств

**Шаг 2: Откройте историю**
- Нажмите на ссылку "История управления устройством" под устройством 1
- URL: `http://localhost:8000/history.php?device_id=1`

**Что вы должны увидеть на странице истории:**
1. ✅ **Заголовок:** "История управления устройством: [имя устройства]"
2. ✅ **Ссылка назад:** "← Вернуться к списку устройств"
3. ✅ **Таблица с историей:**
   - Колонки: "Дата и время" | "Пользователь" | "Действие"
   - Строки с вашими действиями:
     - "Включить реле"
     - "Выключить реле"
   - Дата и время каждой операции

### Что проверить в БД:
```sql
SELECT * FROM user_actions WHERE DEVICE_ID = 1 ORDER BY DATE_TIME DESC;
```
Должны быть записи о всех ваших действиях.

---

## 📋 Задание 6: Блокировка пользователя

**URL:** `http://localhost:8000/task6_index.php`

### Что вы должны увидеть (НОРМАЛЬНО):
1. ✅ **То же, что в задании 5** (устройства с историей)

### Как проверить блокировку:

**⚠️ Для быстрого тестирования (5 секунд вместо 1 минуты):**
- Выполните SQL-скрипт `update_triggers_fast.sql` в MySQL
- Теперь блокировка сработает при **> 3 действий за 5 секунд** (вместо 10 за минуту)

**Шаг 1: Быстрое выполнение действий (имитация атаки)**
- Откройте `task6_index.php`
- **Очень быстро нажмите кнопки более 3 раз за 5 секунд:**
  - Откройте страницу в 2-3 вкладках браузера
  - Нажимайте кнопки "Включить реле" или "Выключить реле" в разных вкладках быстро
  - Сделайте минимум 4 нажатия в течение 5 секунд

**Шаг 2: Попытка доступа после блокировки**
- Обновите страницу `task6_index.php`
- **Что вы должны увидеть:**
  - ❌ **КРАСНОЕ СООБЩЕНИЕ:** "Доступ запрещен"
  - ❌ **Текст:** "Ваш аккаунт заблокирован из-за чрезмерно частых обращений к устройству."
  - ❌ **Устройства НЕ отображаются**

### Что проверить в БД:
```sql
SELECT USER_ID, IS_BLOCKED, BLOCKED_UNTIL FROM users WHERE USER_ID = 1;
```
После блокировки: `IS_BLOCKED = 1`, `BLOCKED_UNTIL` установлено на время через 30 секунд (при использовании `update_triggers_fast.sql`) или 5 минут (при стандартных настройках).

**Разблокировка:**
- Подождите 30 секунд (при быстрых триггерах) или 5 минут (стандартные) ИЛИ выполните:
```sql
UPDATE users SET IS_BLOCKED = 0, BLOCKED_UNTIL = NULL WHERE USER_ID = 1;
```

---

## 📋 Задание 7: Блокировка устройства

**URL:** `http://localhost:8000/task7_index.php`

### Что вы должны увидеть (НОРМАЛЬНО):
1. ✅ **То же, что в задании 5** (устройства с историей)

### Как проверить блокировку устройства:

**⚠️ Для быстрого тестирования (5 секунд вместо 30 секунд):**
- Выполните SQL-скрипт `update_triggers_fast.sql` в MySQL
- Теперь блокировка устройства сработает при **> 3 обращений за 5 секунд** (вместо 5 за 30 секунд)

**Шаг 1: Частые обращения к одному устройству**
- Откройте `task7_index.php`
- **Очень быстро нажмите кнопки для ОДНОГО устройства более 3 раз за 5 секунд** (при быстрых триггерах) или более 5 раз за 30 секунд (стандартные настройки)

**Шаг 2: Проверка блокированного устройства**
- Обновите страницу
- **Что вы должны увидеть:**
  - ✅ **Обычные устройства** отображаются нормально
  - ⚠️ **Заблокированное устройство** в **КРАСНОЙ РАМКЕ:**
    - Заголовок: "Устройство: [имя] (ID: X)"
    - **КРАСНЫЙ ТЕКСТ:** "⚠ ВНИМАНИЕ: Это устройство заблокировано из-за подозрительного поведения (чрезмерно частые обращения к серверу)."
    - Температура и реле отображаются (только для просмотра)
    - **Кнопки управления ОТСУТСТВУЮТ**
    - Текст: "Управление устройством недоступно."

### Что проверить в БД:
```sql
SELECT DEVICE_ID, NAME, IS_BLOCKED FROM device_table WHERE DEVICE_ID = 1;
```
После блокировки: `IS_BLOCKED = 1`

**Разблокировка устройства:**
```sql
UPDATE device_table SET IS_BLOCKED = 0 WHERE DEVICE_ID = 1;
```

---

## 📋 Задание 8: Защита от SQL-инъекций

**URL:** `http://localhost:8000/task8_index.php`

### Что вы должны увидеть:
1. ✅ **То же, что в задании 7** (устройства, с блокировками)

### Как проверить защиту:

**Шаг 1: Попытка SQL-инъекции через URL**
Попробуйте открыть с вредоносными параметрами:
```
task8_index.php?device_id=1' OR '1'='1
history.php?device_id=1'; DROP TABLE users; --
```

**Ожидаемый результат:**
- ✅ **Страница должна работать нормально** (не должна упасть)
- ✅ **Параметры игнорируются или обрабатываются безопасно**
- ✅ **Данные не должны быть повреждены**

**Шаг 2: Проверка валидации входных данных**
- Откройте `task8_index.php`
- Нажмите кнопки → должно работать нормально
- Проверьте историю → должна работать нормально

**Шаг 3: Проверка кода защиты**
- Откройте `task8_index.php` в редакторе
- Найдите функцию `sanitizeInput()` → должна валидировать входные данные
- Все SQL-запросы должны использовать **prepared statements** (`:id`, `:command` и т.д.)

### Что проверить в коде:
- ✅ Все `$pdo->prepare()` используют параметры (`:id`, `:device_id`)
- ✅ НЕТ строк типа: `"SELECT * FROM table WHERE id = " . $_GET['id']`
- ✅ Используется `htmlspecialchars()` для вывода данных
- ✅ Используется `intval()` или `floatval()` для числовых параметров

---

## 📋 Дополнительные страницы

### API для устройства: `device_status.php`

**URL:** `http://localhost:8000/device_status.php?ID=1&Term=25&Rele=1`

**Что вы должны увидеть:**
- ✅ **Текст:** `COMMAND 1 EOC` или `COMMAND 0 EOC` (команда для устройства)
- ❌ **НЕТ HTML** (только текст)

**Как проверить:**
- Откройте в браузере → должен быть только текст команды
- Попробуйте без параметров → должно быть `COMMAND ? EOC`

---

### Главная страница: `tasks_index.php`

**URL:** `http://localhost:8000/tasks_index.php`

**Что вы должны увидеть:**
- ✅ Красивый список всех 8 заданий
- ✅ Ссылки на каждое задание
- ✅ Статус "✓ Выполнено" для каждого задания

---

## ✅ Чек-лист проверки

- [ ] Задание 1: Базовое приложение работает, кнопки управляют устройством
- [ ] Задание 2: Код разделен на модули, интерфейс стилизован
- [ ] Задание 3: Отображаются все устройства, каждое управляется отдельно
- [ ] Задание 4: Устройство можно выбрать по токену в URL
- [ ] Задание 5: Есть ссылка на историю, история показывает действия
- [ ] Задание 6: Пользователь блокируется при частых действиях
- [ ] Задание 7: Устройство блокируется при частых обращениях, показывается предупреждение
- [ ] Задание 8: Защита от SQL-инъекций работает, код использует prepared statements

---

## 🐛 Частые проблемы

**"Не могу увидеть устройство"**
- Добавьте данные: `device_status.php?ID=1&Term=25&Rele=1`

**"История пуста"**
- Выполните несколько действий через кнопки на странице

**"Триггеры не работают"**
- Проверьте, что выполнили `database_schema_update.sql`
- Проверьте: `SHOW TRIGGERS;` в MySQL

**"Ошибка подключения к БД"**
- Проверьте `config.php` (параметры подключения)
- Убедитесь, что MySQL запущен
- Проверьте, что база `bd_lab10-2` существует

